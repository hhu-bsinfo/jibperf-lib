def pomConfig = {
    licenses {
        license {
            name 'GNU General Public License, Version 3.0'
            url 'https://www.gnu.org/licenses/gpl-3.0.txt'
            distribution "repo"
        }
    }

    developers {
        developer {
            id 'faruh100'
            name 'Fabian Ruhland'
            email 'fabian.ruhland@uni-duesseldorf.de'
        }
    }

    scm {
        url 'https://github.com/hhu-bsinfo/jibperf-lib'
    }
}

publishing {
    publications {
        mavenPublication(MavenPublication) {

            from components.java

            groupId project.group
            artifactId project.name
            version project.version

            pom.withXml {
                def root = asNode()
                root.appendNode('description', 'JNI-wrapper for ibperf-lib (https://github.com/hhu-bsinfo/ibperf-lib)')
                root.appendNode('name', 'jibperf-lib')
                root.appendNode('url', 'https://github.com/hhu-bsinfo/jibperf-lib')
                root.children().last() + pomConfig
            }
        }
    }
}